{
  "name": "infohub-challenge",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm run build",
    "start": "node server/server.js",
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm run dev\""
  },
  "engines": {
    "node": "22.x"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.6",
    "tailwindcss": "^3.4.18",
    "concurrently": "^8.2.0"
  }
}
